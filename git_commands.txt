7197767+wbsalls@users.noreply.github.com

git (shows all commands)

git clone

git status
git add <file.extension>
OR git add -A (adds all ?edited? files)
git commit -m “<comments / what you did>”
git push

git pull

git fetch -v --dry-run (like status, but checks remote repository)

git checkout <file.extension> (reverts back to version on master branch)